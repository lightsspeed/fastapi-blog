# ⚠️ IMPORTANT: Do NOT commit real secrets to Git.
#
# This is a TEMPLATE. Replace the base64-encoded placeholder values with your
# actual secrets before applying.
#
# To base64-encode a value (Linux/macOS):
#   echo -n "mysecretvalue" | base64
#
# To apply manually:
#   kubectl apply -f k8s/secret.yaml
#
# Better: manage secrets via a secrets manager (e.g. AWS Secrets Manager,
# HashiCorp Vault, Sealed Secrets, External Secrets Operator).

apiVersion: v1
kind: Secret
metadata:
  name: fastapi-app-secret
  namespace: fastapi-app
  labels:
    app: fastapi-app
type: Opaque
data:
  # Replace with: echo -n "your-secret-key" | base64
  SECRET_KEY: eW91ci1zZWNyZXQta2V5

  # Replace with: echo -n "postgresql://user:pass@host:5432/db" | base64
  DATABASE_URL: cG9zdGdyZXNxbDovL3VzZXI6cGFzc0Bob3N0OjU0MzIvZGI=

  # Replace with: echo -n "your-api-key" | base64
  API_KEY: eW91ci1hcGkta2V5
