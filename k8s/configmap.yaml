apiVersion: v1
kind: ConfigMap
metadata:
  name: fastapi-app-config
  namespace: fastapi-app
  labels:
    app: fastapi-app
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  APP_PORT: "8000"
  # Add additional non-sensitive environment variables here
  # ALLOWED_ORIGINS: "https://yourdomain.com"
